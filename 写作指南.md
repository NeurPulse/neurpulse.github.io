# 写作指南

写博客笔记时的注意事项清单。发布流程见 [README.md](README.md)。

---

## 一、必须注意（否则会出问题）

### 1. frontmatter 格式

> 💡 不用手敲：运行 `npm run new` 回答几个问题，自动生成带当天日期的完整 frontmatter 并用 Typora 打开。以下规则供你理解/手动修改时参考。

每篇文章开头的元信息，格式严格：

```markdown
---
title: '文章标题'
description: '一句话描述'
pubDate: 2026-08-07
category: '深度学习'
tags: ['PyTorch', '自动求导']
---
```

- `---` 必须**顶格**，字段用英文冒号
- `pubDate` 严格用 `YYYY-MM-DD` 格式，**写错会导致整个网站构建失败**
- 标题含特殊字符时用单引号包住

### 2. 图片路径

| 写法 | 结果 |
|---|---|
| `C:/Users/.../xxx.png`（本地绝对路径） | ❌ 线上裂图 |
| `http://xxx.com/img.png`（http 外链） | ❌ 浏览器拦截 |
| `/images/xxx.png`（放 `public/images/` 里） | ✅ 正确 |

**推荐**：Typora → 偏好设置 → 图像 → 插入图片时"复制图片到 `./${filename}.assets` 文件夹"。写完把 md 文件和 assets 文件夹一起发给 Claude 处理即可。

### 3. 公式里的竖线 `|`

表格中写条件概率、条件期望时，`|` 会被误认为表格分隔符：

```markdown
| 公式 | 含义 |
|---|---|
| $P(A\|B)$ | 条件概率 |   ← 必须写成 \|
```

---

## 二、显示效果（建议遵守）

### 4. 公式块规范

```markdown
✅ 正确：$$ 顶格，上下留空行

$$
MSE = \frac{1}{n}\sum_{i=1}^{n} \left(y_i - \hat{y}_i\right)^2
$$

❌ 避免：$$ 前面有大段空格（Typora 自动加的，可能渲染成代码块）
```

### 5. 标题层级

- 正文从 `##` 开始（`#` 已由文章标题占用，不要再写）
- 右侧目录收录 `##`、`###`、`####` 三级，层级越深得越多缩进；`#####` 及以下不进目录
- 想让某节出现在目录里，至少要用 `####`；不想进目录的小标题用 `**加粗**` 代替

### 6. Typora 隐形字符

Typora 偶尔插入零宽空格（看不见），导致某段渲染怪异却找不到原因——把那段删掉重打一遍通常就好了。

---

## 三、好习惯（长期受益）

### 7. 分类名固定

`category` 从下面几个里选，**不要自创新词**（"深度学习"和"深度 学习"会变成两个分组）：

- `数学建模` —— 数值计算、蒙特卡洛、微分方程数值解
- `深度学习` —— 神经网络基础、损失函数、反向传播
- `PyTorch` —— PyTorch 语法与实践
- `Transformer` —— 注意力机制、架构
- `论文精读` —— 仅用于 `research/` 目录

**如何新增分类**：

网站的分类不是"注册制"——frontmatter 里写任何分类名都会自动生成分组。`npm run new` 菜单里的清单只是为了防止打错字。想加新分类，打开 `scripts/new-post.mjs`，找到开头的数组加一项即可：

```js
const CATEGORIES = ['数学建模', '深度学习', 'PyTorch', 'Transformer', '强化学习', '论文精读', '未分类'];
//                                                                  ↑ 新分类加在这里
```

保存后立即生效，无需重启。加完请**回到本页更新上面的清单**，保持文档和实际一致。

注意：**改菜单里的分类名不会影响旧文章**——旧文章的分类写在各自 frontmatter 的 `category` 字段里，想改要逐篇修改再 push。

### 8. 一篇一个主题

一篇讲透一个知识点（如"自动求导"一篇、"DataLoader"一篇），不要把整个章节塞进一篇。好处：目录导航有意义、搜索更准、阅读进度感更强。

### 9. 先发布，后打磨

写到七八成就推送上线，之后随时改随时再推。不想公开的半成品加 `draft: true`。

### 10. 长文边写边预览

终端运行 `npm run dev`，浏览器开 http://localhost:4321 ，保存文件自动刷新，公式对不对当场可见。

---

## 四、发布速查

```bash
git add .
git commit -m "添加文章：xxx"
git push
```

等 1~2 分钟刷新网站。没变化就 `Ctrl + Shift + R` 强制刷新；还没有就去 [Actions](https://github.com/NeurPulse/neurpulse.github.io/actions) 看是不是构建失败了。

**偷懒方式**：写完直接把 md 文件发给 Claude 说"帮我发布"，格式检查、图片处理、推送全包。
